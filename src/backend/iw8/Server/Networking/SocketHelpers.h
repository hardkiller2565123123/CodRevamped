#pragma once

// Module boundary for Server/Networking/SocketHelpers.
// Its implementation is compiled through Server.cpp so legacy private helper
// linkage/state remains unchanged while the source is physically separated.
