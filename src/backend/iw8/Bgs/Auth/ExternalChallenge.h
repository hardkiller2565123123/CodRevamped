#pragma once

// Module boundary for Bgs/Auth/ExternalChallenge.
// Its implementation is compiled through ServerTls.cpp so legacy private helper
// linkage/state remains unchanged while the source is physically separated.
